services:
  - type: web
    name: whatsapp-chatbot
    env: java
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/whatsapp-chatbot-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
    healthCheckPath: /webhook/health

